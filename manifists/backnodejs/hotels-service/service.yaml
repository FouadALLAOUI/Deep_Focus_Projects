apiVersion: v1
kind: Service
metadata:
  name: hotels-service
  namespace: hotels-service
  labels:
    app: hotels-service
spec:
  type: NodePort
  selector:
    app: hotels-service
  ports:
    - name: http
      port: 80
      targetPort: 3002
