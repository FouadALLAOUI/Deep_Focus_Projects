apiVersion: apps/v1
kind: Deployment
metadata:
  name: hotels-service-deployment
  namespace: hotels-service
  labels:
    app: hotels-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hotels-service
  template:
    metadata:
      labels:
        app: hotels-service
    spec:
      containers:
        - name: hotels-service
          image: fouadallaoui/hotels-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3002
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3002"
            - name: OPENTRIPMAP_API_KEY
              valueFrom:
                secretKeyRef:
                  name: hotels-service-secrets
                  key: opentripmap-api-key
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3002
            initialDelaySeconds: 30
            periodSeconds: 30
