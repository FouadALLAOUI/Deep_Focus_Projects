# Template — fill in real values before applying.
# Apply manually: kubectl apply -f secret.yaml
# For production, use Sealed Secrets or External Secrets Operator instead.
apiVersion: v1
kind: Secret
metadata:
  name: hotels-service-secrets
  namespace: hotels-service
type: Opaque
stringData:
  opentripmap-api-key: "REPLACE_WITH_YOUR_FREE_KEY_FROM_opentripmap.com"
