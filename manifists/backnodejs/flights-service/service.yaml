apiVersion: v1
kind: Service
metadata:
  name: flights-service
  namespace: flights-service
  labels:
    app: flights-service
spec:
  type: NodePort
  selector:
    app: flights-service
  ports:
    - name: http
      port: 80
      targetPort: 3004
