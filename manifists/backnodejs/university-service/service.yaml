apiVersion: v1
kind: Service
metadata:
  name: university-service
  namespace: university-service
  labels:
    app: university-service
spec:
  type: NodePort
  selector:
    app: university-service
  ports:
    - name: http
      port: 80
      targetPort: 3001
