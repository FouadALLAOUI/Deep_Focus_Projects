apiVersion: v1
kind: Service
metadata:
  name: frontnextjs-service
  labels:
    app: frontnextjs
spec:
  type: NodePort #LoadBalancer #ClusterIP #NodePort
  selector:
    app: frontnextjs
  ports:
    - name: http
      port: 80
      targetPort: 3000

