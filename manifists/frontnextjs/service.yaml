apiVersion: v1
kind: Service
metadata:
  name: frontnextjs-service
  namespace: frontnextjs
  labels:
    app: frontnextjs
spec:
  type: NodePort
  selector:
    app: frontnextjs
  ports:
    - name: http
      port: 80
      targetPort: 3000
